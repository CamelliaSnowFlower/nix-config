{config, ...}: {
  
  programs.nvf.vim.filetree.nvimTree = {
    enable = true;
    setupOpts = {
      actions ={};
      diagnostics = {};
      filters = {
        dotfiles = true;
        exclude = [];
        git_clean = false;
        git_ignored = false;
        no_buffer = false;
      };
      git.enable = true;
      hijack_cursor = true;
      modified = {
        enable = true;
      };
      view = {
        float = {
          enable = true;
          # open_win_config ={};
        };
        preserve_window_proportions = false;
        relativenumber = true;
      };
    };
  };
}
